# -*- mode: shell-script -*-
# vim: set ft=zsh :

if ! command -v Xephyr &> /dev/null; then
    return 1
fi

xephyr()
{
    if [ -z "$1" ]; then
        1=:1
    fi
    if [ -z "$2" ]; then
        2=800x600
    fi
    Xephyr $1 -screen $2 -reset 2> /dev/null &
    sleep 1
    DISPLAY=$1 zsh
}
