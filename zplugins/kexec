# -*- mode: shell-script -*-
# vim: set ft=zsh :

if ! (( $+commands[kexec] )); then
    return 1
fi

kexecready()
{
	kexec -l /boot/vmlinuz-linux --append="root=/dev/sda1 ro resume=/dev/sda5 $1" --initrd=/boot/initramfs-linux.img
	rc.d start kexec
}
