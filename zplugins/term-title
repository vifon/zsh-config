# -*- mode: shell-script -*-
# vim: ft=zsh fdm=marker :

if [ "$TERM" = "linux" ]; then
    return 0;
fi

set-term-title-dir()
{
    print -n "\e]2;urxvt: `print -Pn %~`\a"
}

set-term-title-dir               # set the title on start
chpwd_functions=(${chpwd_functions} set-term-title-dir)
